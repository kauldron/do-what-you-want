name: gradlew_earlybird_feature_dev

on:   
  pull_request:
    branches: [dev]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8
      - name: clone EarlyBird
        run: git clone https://github.com/americanexpress/earlybird.git
      - name: install EarlyBird
        run: $GITHUB_WORKSPACE/earlybird/build.sh && $GITHUB_WORKSPACE/earlybird/install.sh
      - name: EarlyBird launch
        run: ./go-earlybird --path=$GITHUB_WORKSPACE
